"""UppASD benchmark harness.

WP-01 provides the data contract only: schema loading and record validation.
Production execution (run isolation, timing, provenance capture) is added by
later work packages.
"""
