// Replaced by test_gpu_dipole_fft.cpp.  That executable is a CMake/CTest
// target and validates complete-kernel convolution; this file remains only to
// avoid silently reviving the former ad-hoc driver interface.
int main() { return 0; }
